# t22-no-panic — library code has no panic site

2026-09-06. Owner instruction: the public API never panics. The
contract is `testing.md` T22. The comment pass
(`comments-pass.md`) listed the sites by hand. `cargo clippy` with the
restriction lints counted them per library crate before any change:

| Crate | `unwrap`/`expect` | `panic!` and `unreachable!` | indexing | slicing |
|---|---|---|---|---|
| `subscript-typegpu-gen` | 3 | 0 | 93 | 3 |
| `subscript-typegpu-harness` (lib) | 4 | 2 | 96 | 5 |
| `subscript-typegpu-webgpu-gen` | 41 | 13 | 26 | 4 |
| `subscript-typegpu-facade` | 0 | 0 | 2 | 2 |

`clippy::arithmetic_side_effects` counted 87, 90, 42, and 3 more.
Arithmetic overflow is outside T22 and stays recorded here.

The rounds: 1 `typegpu-gen`, 2 `harness` and `facade`, 3
`webgpu-gen`. Each round lands the lint denial in the crate root, so
the gate holds the crate from that commit on.

## Landed (2026-09-06)

| Round | Commit | Gate (`--require-backend`, this host) |
|---|---|---|
| 1 `typegpu-gen` | `3a57893` | green, 277 passed, 1 ignored, 220.7 s |
| 2 `harness`, `facade` | `baad7b7` | green, 281 passed, 1 ignored, 223.4 s |
| 3 `webgpu-gen` | the commit above | green, 283 passed, 1 ignored, 238.6 s |

Every library crate root denies the seven lints, and every `allow`
sits in a `#[cfg(test)]` module. Round 1 added `internal(site, what,
pos)` to `crates/typegpu-gen/src/lib.rs`, and the diagnostics test
exempts the `internal:` prefix and rejects a fixture that expects it.
Round 2 made `run_on_compiler_stack` and the program pool return
errors, and `coverage_hit` ignores an index outside the counter
slice. Two `typegpu-gen` tests that consumed the harness output moved
to the `Result`. Round 3 made the generator return `internal:` errors
where it panicked on its own output, and changed one emitter template
so `crates/facade/src/generated.rs` no longer slices (three lines in
the committed output, regenerated byte-identically otherwise), which
removed the one `allow` on a generated module. Every `.wgsl` and
`.expected` golden is byte-identical across the three rounds. The
six new tests pin the error paths that were panics.
